import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['dummyimage.com','picsum.photos', 'placekitten.com'], // add any other external domains you use here
  },
};

export default nextConfig;
